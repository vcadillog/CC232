## BLOQUE 1

1. Un problema es algo a ser resuelto, un algoritmo es una secuencia de procedimientos logicos para resolver un problema, implementacion es la forma explicita de resolver un problema, un ADT es la representacion de las operaciones.
2. lastSwapIndex : el ultimo indice intercambiado, swaps costo
3. Power opera desplazando los bits a la derecha hasta llegar a 1 
4. Fibonnaci vuelve a calcular todos los valores por cada recursion
5. 
6. intsequence es un adt fixed un array y vectorsequence trabaja con un vector y puede funcionar
porque hace polimorfismo sobre el mismo adt.

## BLOQUE 2

| archivo | salida | idea algoritmica | costo |
| demo_bubblesort.cpp | comparaciones = 14, intercambios = 7, ultimo swap = 0 |  | stats.swaps |
1. stats.swaps mide la cantidad de intercambios
2. 
3.


## BLOQUE 3

1. sum, power, fib, la cantidad de elementos de fib coincida con la salida.
2. Demuestra comportamientos esperados del codigo.
3. No demuestra que la respuesta sea correcta, sino comportamientos coherente.
4. 

40. Ordena y comenta estos crecimientos de menor a mayor:
   - O(1),
   - O(log n),
   - O(n),
   - O(n^2),
   - O(2^n).

   - O(1), O(logn), O(n), O(n^2), O(2^n)
41. ¿Por qué una diferencia pequeña en la fórmula puede ser enorme cuando `n` crece?
    Porque cambios pequeños en valores grandes de n tiene salidas de O(f(n)) más grandes.
42. ¿Qué papel juega el tamaño de entrada en el análisis?
    Porque al trabajar con tamaños de entrada maś grandes o pequeños tienen impacto mayor en los costos de ejecución, crecen de forma f(n). 
43. ¿Por qué los algoritmos exponenciales suelen volverse inviables rápidamente?
    Porque los costos se vuelven exponenciales, ya sean temporales o memoria, aunque las capacidades de memoria sean grandes, los límites temporales no podrían ser asumidos.
44. ¿Qué significa decir que existe una jerarquía de complejidades?
    Clasifica los algoritmos según su notación O, mientras más complejos, son más costosos, se ordenan los f(n) de menor a mayor.


